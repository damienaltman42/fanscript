{
  "name": "fanscript",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "pnpm --parallel dev",
    "build": "pnpm --parallel build",
    "lint": "pnpm --parallel lint"
  },
  "devDependencies": {
    "typescript": "^5.3.0"
  }
}
